Currently working features:
Websockets (networking)
Login functionality
Create an account
Restrictions based on account access level
Dynamically loaded data tables

Currently non-working features:
Geolocation isn't stored in the backend
Form validation isn't present in the frontend for account creation
Columns containing links in any table are not loaded, but the rest of the columns are fine
The number column in the Lectures table is inaccurate. What appears on the site is actually what is in the database
The Edit profile page is completely non-functional
The multi-threading portion of the app is non-functional