import { LimitAccessDirective } from './limit-access.directive';

describe('LimitAccessDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitAccessDirective();
    expect(directive).toBeTruthy();
  });
});
